
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.msa6706.aes256',
  appName: 'AES256 Offline App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
